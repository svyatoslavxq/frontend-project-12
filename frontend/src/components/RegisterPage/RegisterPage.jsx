import { Link } from 'react-router-dom';

const RegisterPage = () => (
  <div>
    <h1>Регистрация</h1>
    <p>
      <Link to="/login">Войти</Link>
    </p>
  </div>
);

export default RegisterPage;
